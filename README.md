To Run the file You have to follow the following steps:-
1. Install Node.Js and VS code
2. 
3. Open VS code terminal and Run the following code:----

npm create vite@latest smart-traffic -- --template react
cd smart-traffic
npm install

4.Open the project in VS Code

5.Run it :---

npm run dev
